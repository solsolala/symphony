replicaCount: 1

image:
  repository: symphony
  pullPolicy: IfNotPresent
  tag: "latest"

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  annotations: {}
  name: "symphony-orchestrator"

podAnnotations: {}

podSecurityContext: {}

securityContext: {}

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: false
  className: ""
  annotations: {}
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []

resources: {}

nodeSelector: {}
tolerations: []
affinity: {}

# Symphony specific config
tracker:
  kind: jira
  endpoint: ""
  projectSlug: ""
  apiKeyEnv: JIRA_API_TOKEN
  assigneeEnv: ""
  activeStates:
    - Todo
    - In Progress
  terminalStates:
    - Closed
    - Cancelled
    - Canceled
    - Duplicate
    - Done

repository:
  cloneUrl: ""
  ref: "main"

codex:
  command: "codex app-server"

runtimeServer:
  host: "0.0.0.0"
  port: 4000

mongo:
  collection: "user_sessions"
  poolSize: 5

env:
  - name: PORT
    value: "4000"
  - name: SYMPHONY_DEFAULT_JIRA_BASE_URL
    value: ""
    # Optional. Prefills the login form with your Jira Cloud or internal Jira base URL.
  - name: SYMPHONY_DEFAULT_GITHUB_BASE_URL
    value: ""
    # Optional. Prefills the login form with your GitHub or GitHub Enterprise base URL.
  - name: JIRA_API_TOKEN
    value: ""
    # Or point tracker.apiKeyEnv at another env var.
    # Prefer loading from a secret:
    # valueFrom:
    #   secretKeyRef:
    #     name: jira-secret
    #     key: api-key
  - name: JIRA_BASE_URL
    value: ""
    # Optional deployment-wide Jira base URL fallback for worker pods.
  - name: OPENAI_API_KEY
    value: ""
    # Worker pods inherit OPENAI_API_KEY by default and need it for real Codex turns.
    # Prefer loading from a secret in non-local environments.
  - name: GITHUB_TOKEN
    value: ""
    # Optional deployment-wide fallback for private GitHub clone/push access.
  - name: GH_TOKEN
    value: ""
    # Optional alias for GitHub CLI-compatible auth.
  - name: GITHUB_SERVER_URL
    value: ""
    # Optional deployment-wide GitHub or GitHub Enterprise web base URL.
  - name: GITHUB_API_URL
    value: ""
    # Optional deployment-wide GitHub or GitHub Enterprise API base URL.
  - name: MONGODB_URI
    value: ""
    # User-scoped Jira/GitHub/Codex session metadata is persisted to MongoDB.
    # Prefer loading from a secret in non-local environments.

workspaceRoot: "/app/workspaces"

worker:
  image:
    repository: symphony
    tag: "latest"
    pullPolicy: IfNotPresent
  inheritEnv:
    - JIRA_BASE_URL
    - JIRA_API_TOKEN
    - GITHUB_TOKEN
    - GH_TOKEN
    - GITHUB_SERVER_URL
    - GITHUB_API_URL
    - OPENAI_API_KEY
    - OPENAI_BASE_URL
    - OPENAI_ORG_ID
  resources: {}
  nodeSelector: {}
  tolerations: []
  affinity: {}
